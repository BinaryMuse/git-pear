{
  "name": "git-pear",
  "main": "./lib/git-pear",
  "version": "0.0.0",
  "description": "Set GIT_AUTHOR and GIT_COMMITTER for pairing when committing from Atom",
  "keywords": [
  ],
  "repository": "https://github.com/atom/git-pear",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  },
  "configSchema": {
    "pairs": {
      "description": "A comma-separated list of possible authors, in the format of 'initials;email', e.g. 'mt;michelle@github.com'",
      "type": "array",
      "default": [""],
      "items": {
        "type": "string"
      }
    }
  }
}
